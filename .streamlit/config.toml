# 38DN institutional theme (Phase 5). Matches assets/theme.css tokens.
[theme]
base = "light"
primaryColor = "#0E2841"        # navy — primary brand
backgroundColor = "#F8FAFC"     # page background (neutral slate-50)
secondaryBackgroundColor = "#FFFFFF"  # card surfaces
textColor = "#0E2841"           # primary ink
font = "sans serif"             # overridden to Inter via theme.css
